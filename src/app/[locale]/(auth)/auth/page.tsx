import { AuthComponent } from './_component/AuthComponent';

const Auth = async () => {
  return (
    <div className='h-full'>
      <div>
        <AuthComponent />
      </div>
    </div>
  );
};

export default Auth;
